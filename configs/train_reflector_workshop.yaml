extends: base_workshop.yaml
experiment:
  name: memory_reflector_workshop
train:
  sequence_len: 4
  use_trajectory_unroll: true
ablation:
  use_memory: true
  use_reflector: true
  use_explain_loss: true
