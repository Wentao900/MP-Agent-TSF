# IMTSA research-ts — Python >= 3.10
# Install: pip install -r requirements.txt && pip install -e .
#
# GPU (CUDA 12.8): after the lines below, run:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# CPU-only fallback is satisfied by torch>=2.2 from PyPI.

# --- Core experiment (parquet pipeline, train, backtest, stats) ---
numpy>=1.25,<3
pandas>=2.0
pyarrow>=14.0
PyYAML>=6.0
scikit-learn>=1.4
joblib>=1.3
scipy>=1.11
torch>=2.2

# --- Editable install needs setuptools ---
setuptools>=68

# --- Tests ---
pytest>=8.0

# --- Optional: data fetch / workshop scripts (crawl, AkShare, etc.) ---
akshare>=1.16.0
requests>=2.31.0
